<h1><%= @ug.user.name %>'s Game Page for '<%= @ug.game.title %>'</h1>
<div class="content-wrapper-left" style="max-width: 500px;">
    <p>Hours Played: <%= @ug.hours_played %></p>
    <p>Rating: <%= @ug.rating %></p>
    <p>Review: <%= @ug.review %></p>
    <p><%= button_to 'Edit Game Page', edit_played_path(slug: @ug.game.slug), method: :get, class: "button" %></p>
    <p><%= button_to 'Delete Game Page', played_path(slug: @ug.game.slug), method: :delete, class: "button", data: { confirm: 'Are you sure you want to delete your game page?' } %></p>
</div>