<%= render "layouts/flash" %>
<h1><%= @user.name %>'s Game Page for '<%= @ug.game.title %>'</h1>
<div class="content-wrapper">
    <%= form_for @ug, url: played_path do |f| %>
        <%= render partial: "form", locals: {f: f} %>
    <% end %>
</div>